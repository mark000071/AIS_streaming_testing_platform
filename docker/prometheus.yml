global:
  scrape_interval: 15s

scrape_configs:
  - job_name: pipeline
    static_configs:
      - targets: ["ingest-fi:9100", "replay:9100", "tracker:9100", "reconcile:9100", "jobs:9100"]
  - job_name: api
    metrics_path: /metrics/
    static_configs:
      - targets: ["api:8000"]
